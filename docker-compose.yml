version: '3.1'

services:
    bot:
        image: gamelinter/discord-music-master:latest
        env_file: .env
        restart: always
        networks:
            - bot-network

networks:
    bot-network:
